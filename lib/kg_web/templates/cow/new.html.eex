<h1>Create Cow</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.cow_path(@conn, :create)) %>

<span><%= link "Cancel", to: Routes.admin_page_path(@conn, :index), class: "btn btn-danger" %></span>
